:javascript
  $(function(){
    $("#date_current_chousen_year").change(function(){
      $(this).change_year();
    });
  });

#tray.box
  %p.box.f-right
  -if @current_user
    Роль:
    %strong=link_to @current_user.human_role, @current_user
    &nbsp;&nbsp;&nbsp;
    -if @current_user.admin?
      \|
      &nbsp;&nbsp;&nbsp;
      %strong=link_to "Регистрация нового пользователя", signup_path
  -if @current_user 
    %p.f-right
      Год
      &nbsp;
      =select_year(session[:choice_year], :start_year=>Time.now.year, :end_year=>1990, :field_name=>'current_chousen_year')
      &nbsp;      
      -if @current_user
        Вы:
        %strong=@current_user.login
        &nbsp;&nbsp;&nbsp;&nbsp;&nbsp;
        %strong=link_to "Выход",logout_path,:id=>"logout"
      -else      
        &nbsp;&nbsp;&nbsp;&nbsp;&nbsp;
        %strong=link_to "Вход",login_path,:id=>"login"
